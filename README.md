# xml-backend-web-project-
An XML-based Product Catalog System built with ASP.NET Core and React. It uses XML files instead of a traditional database and includes XSD validation, XSLT transformation, JWT authentication, and RESTful API operations for managing products.
